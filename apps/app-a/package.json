{
  "name": "app-a",
  "scripts": {
    "build": "mkdir -p dist && echo \"Your application output!\" > dist/app-output.txt && echo \"Application A is built!\""
  },
  "dependencies": {
    "pkg-a": "workspace:*"
  }
}
